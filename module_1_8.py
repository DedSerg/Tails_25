
# Создаем кортеж immutable_var
immutable_var = (42, "Hello", 3.14, True)

# Выводим кортеж на экран
print("Кортеж immutable_var:", immutable_var)

# 3. Изменение значений переменных
try:
    # Попытка изменить элемент кортежа
    immutable_var[0] = 100  # Это вызовет ошибку
except TypeError as e:
    print("Ошибка:", e)
    print("Нельзя изменить значения элементов кортежа, так как кортежи являются неизменяемыми (immutable) объектами в Python.")

# 4. Создание изменяемых структур данных
# Создаем список mutable_list
mutable_list = [1, 2, 3, 4, 5]

# Изменяем элементы списка
mutable_list[0] = 10  # Изменяем первый элемент
mutable_list[1] = 20  # Изменяем второй элемент

# Выводим измененный список на экран
print("Измененный список mutable_list:", mutable_list)